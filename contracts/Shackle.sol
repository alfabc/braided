pragma solidity ^0.4.24;


contract Shackle {
  function addChain(uint chainID, bytes32 genesisBlockHash) external {
  }

  function recordBlock(uint chainID, uint blockNumber, bytes32 blockHash) external {
  }
}
